# 🚢 Shipping DocFlow 优化清单

本文档用于记录系统已完成的优化项以及未来的性能/空间提升计划。

## ✅ 已完成优化 (Completed)

| 日期 | 优化项 | 说明 | 提升效果 |
| :--- | :--- | :--- | :--- |
| 2026-04-10 | **前后端目录分离** | 将前端 Vue 项目与后端 Maven 项目分文件夹整理，根目录仅保留元数据。 | 提高维护性，降低目录复杂度。 |
| 2026-04-10 | **工程环境清理** | 删除了冗余的 `.bat` 文件、旧日志、编译产物 (`target`, `dist`) 以及误创建的 `D:` 目录。 | 释放空间 ~50MB+，消除跨平台脚本冲突。 |
| 2026-04-10 | **Mac 兼容性修正** | 修正了 `application.yml` 中的路径配置，移除 Windows 风格驱动器路径。 | 解决 Mac 环境下文件上传/读取失败的问题。 |
| 2026-04-10 | **数据库垂直分表** | 将 `bill_of_lading_v5` 拆分为 `bl_main`, `bl_parties`, `bl_cargo`, `bl_freight`。 | 核心表行大小减小（Row Size < 1KB），提升主表索引性能。 |
| 2026-04-10 | **业务与系统解耦** | 重命名 `sys_pdf_template` 到 `bl_pdf_template`，重构 Entity 为组件模式。 | 降低了业务代码与系统基础表的耦合，增强代码可维护性。 |

## 🚀 待实施优化 (Planned)

## 🚀 待实施优化 (Planned)

### 1. 🕒 日志表水平拆分
*   **目标**: 解决 `sys_oper_log` 和 `sys_logininfor` 长期积累导致查询变慢的问题。
*   **方案**: 按月或按季度进行水平拆分（Sharding）或者开启 MySQL 分区。
*   **预期**: 维持日志查询性能，方便历史日志归档与删除。

### 2. 🖼️ 静态资源与文件存储优化
*   **目标**: 减少后端服务器本地存储压力。
*   **方案**: 引入对象存储（OSS/S3/MinIO），将 PDF 模版和生成的业务文档迁移。
*   **预期**: 方便系统横向扩展，解决多节点文件同步问题。

### 4. 🖼️ 静态资源与文件存储优化
*   **目标**: 减少后端服务器本地存储压力。
*   **方案**: 引入对象存储（OSS/S3/MinIO），将 PDF 模版和生成的业务文档迁移。
*   **预期**: 方便系统横向扩展，解决多节点文件同步问题。

---
*最后更新日期: 2026-04-10*
